<resources>
    <!-- Cold-start-only theme (see MainActivity.onCreate's installSplashScreen call):
         the system paints this for the brief window between process start and the
         first Compose frame, when nothing this app draws itself even exists yet.
         Reuses the adaptive icon's own foreground vector as the animated icon
         (the platform's own recommended source for it) rather than a separate
         asset to keep in sync. postSplashScreenTheme hands off to the real
         theme the manifest already declares at the application level the moment
         the splash screen dismisses. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@android:style/Theme.DeviceDefault.NoActionBar</item>
    </style>
</resources>